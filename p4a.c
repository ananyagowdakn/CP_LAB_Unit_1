//c program to check positive number
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    if (n > 0)
        printf("Positive");
    else
        printf("Not Positive");

    return 0;
}
